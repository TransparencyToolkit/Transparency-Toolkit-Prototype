<%= simple_form_for [plugin_call, plugin_call.plugin_call_fields.build], :html => { :multipart => true, :class => 'form-inline' } do |f| %>

  <div class="form-inputs">
    <%= f.input :test %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
