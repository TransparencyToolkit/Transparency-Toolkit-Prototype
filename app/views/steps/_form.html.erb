<%= form_for(@step) do |f| %>


    <%= f.collection_select :plugin_call_id,  PluginCall.where(category: category).order(:id), :details, :what, include_blank: 'Please select', wrapper_html: {class: 'medium-12 small-12 columns'} %>

    <%= hidden_field_tag :recipe_id %>
    <%= f.submit 'Next: Tool Inputs', :class => 'button modal-form' %>
<% end %>
